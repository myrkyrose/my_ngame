/// add_skin(name, hp, head, body)

skin[count_skin] = ds_map_create();
var map = skin[count_skin++];
map[? 'head'] = argument2;
map[? 'body'] = argument3;
map[? 'hp'] = argument1;
map[? 'name'] = argument0;
